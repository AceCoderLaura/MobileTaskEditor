<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MobileTaskEditor"
             x:Class="MobileTaskEditor.MainPage">

    <ContentPage.BindingContext>
        <local:MainModel />
    </ContentPage.BindingContext>

    <StackLayout>

        <StackLayout IsVisible="{Binding HasTask}">
            <Entry Text="{Binding CurrentTask.Description}" />
            <Label Text="SAVED ✅" IsVisible="{Binding CurrentTaskSaved, FallbackValue=False}" />
            <Button Command="{Binding SaveTaskCommand}" Text="SAVE" />
        </StackLayout>
        
        <Button Command="{Binding NewTaskCommand}" Text="NEW" />

    </StackLayout>

</ContentPage>
