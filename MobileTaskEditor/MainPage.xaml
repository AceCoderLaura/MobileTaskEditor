<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:MobileTaskEditor"
             x:Class="MobileTaskEditor.MainPage">

    <ContentPage.BindingContext>
        <local:MainModel />
    </ContentPage.BindingContext>
    
    <StackLayout>
        
        <Entry Text="{Binding CurrentTask.Description}" />

        <Label Text="SAVED ✅✔☑" IsVisible="{Binding CurrentTask.Saved}" />
        
        <Button Command="{Binding SaveTaskCommand}" />
        <Button Command="{Binding NewTaskCommand}" />
        
    </StackLayout>

</ContentPage>
