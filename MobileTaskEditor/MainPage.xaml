<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewModel="clr-namespace:MobileTaskEditor.ViewModel;assembly=MobileTaskEditor"
             xmlns:mobileTaskEditor="clr-namespace:MobileTaskEditor;assembly=MobileTaskEditor"
             x:Class="MobileTaskEditor.MainPage">

    <ContentPage.BindingContext>
        <viewModel:MainModel />
    </ContentPage.BindingContext>

    <StackLayout>
        
        <StackLayout IsVisible="{Binding HasTask}">
            <Entry Text="{Binding CurrentTask.TaskInfo.Description}" />
            <Label Text="SAVED ✅" IsVisible="{Binding CurrentTask.TaskInfo.IsChanged, Converter={mobileTaskEditor:InverterConverter}, FallbackValue=False}" />
            <Button Command="{Binding CurrentTask.SaveTaskCommand}" Text="SAVE" />
        </StackLayout>

        <Button Command="{Binding NewTaskCommand}" Text="NEW" />

    </StackLayout>

</ContentPage>